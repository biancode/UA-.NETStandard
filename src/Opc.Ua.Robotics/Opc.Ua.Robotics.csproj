<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <AssemblyName>$(AssemblyPrefix).Robotics</AssemblyName>
    <TargetFrameworks>$(LibTargetFrameworks)</TargetFrameworks>
    <PackageId>$(PackagePrefix).Opc.Ua.Robotics</PackageId>
    <RootNamespace>Opc.Ua.Robotics</RootNamespace>
    <NoWarn>$(NoWarn);CS1591;CS0108</NoWarn>
    <Nullable>enable</Nullable>
    <Description>Server/client-independent OPC 40010 Robotics contracts and source-generated Robotics model over the OPC 10000-200 Industrial Automation and OPC 10000-100 DI base models, including model identifiers, typed states and proxies, model loaders, and focused topology read-projection contracts.</Description>
    <IsPackable>true</IsPackable>
    <PackageReadmeFile>NugetREADME.md</PackageReadmeFile>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <IsAotCompatible Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net10.0'))">true</IsAotCompatible>
  </PropertyGroup>
  <PropertyGroup Condition="'$(Configuration)' == 'Debug'">
    <PackageId>$(PackageId).Debug</PackageId>
  </PropertyGroup>
  <ItemGroup>
    <InternalsVisibleTo Include="Opc.Ua.Robotics.Server" />
    <InternalsVisibleTo Include="Opc.Ua.Robotics.Client" />
    <InternalsVisibleTo Include="MinimalRobotServer" />
    <InternalsVisibleTo Include="Opc.Ua.Di.Tests" />
    <InternalsVisibleTo Include="Opc.Ua.Robotics.Tests" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\Opc.Ua.Di\Opc.Ua.Di.csproj" />
    <ProjectReference Include="..\Opc.Ua.IA\Opc.Ua.IA.csproj" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\tools\Opc.Ua.SourceGeneration\Opc.Ua.SourceGeneration.csproj">
      <OutputItemType>Analyzer</OutputItemType>
      <ReferenceOutputAssembly>false</ReferenceOutputAssembly>
    </ProjectReference>
  </ItemGroup>
  <!-- The OPC 40010 Robotics model is source-generated from its NodeSet2 XML.
       Its required OPC 10000-200 IA base model is no longer generated here: it
       moved to src/Opc.Ua.IA when OPC 40001-1 Machinery needed it as well, and
       two assemblies generating the same model URI under the same C# prefix
       collide at the consumer. IA (and DI beneath it) now resolve through the
       referenced assemblies' [assembly: ModelDependencyAttribute] payloads, so
       neither NodeSet is needed here; the generated types keep living in the
       Opc.Ua.IA namespace, so no source change was required.

       The Robotics Programs facet uses the standard FileDirectoryType, whose
       method-state classes a curated Core omits; the model generator degrades
       those absent standard method-state references to the base MethodState
       (see StandardMethodStateFallback), so the model compiles. OmitFluentApi
       keeps the output model-only (Core + DI + IA, no Server dependency). -->
  <ItemGroup>
    <None Remove="Model\Opc.Ua.Robotics.NodeSet2.xml" />
    <None Remove="Model\Opc.Ua.RobotIntent.NodeSet2.xml" />
  </ItemGroup>
  <ItemGroup>
    <AdditionalFiles Include="Model\Opc.Ua.Robotics.NodeSet2.xml">
      <ModelSourceGeneratorPrefix>Opc.Ua.Robotics</ModelSourceGeneratorPrefix>
    </AdditionalFiles>
    <!-- OPC UA - Robot Intent supplies the task-level motion verbs OPC 40010
         leaves undefined. Its NodeSet is standalone on the base UA namespace, so
         it generates independently of the Robotics and DI models above; it lives
         in this project only because this is where the robot-facing API is. -->
    <AdditionalFiles Include="Model\Opc.Ua.RobotIntent.NodeSet2.xml">
      <ModelSourceGeneratorModelUri>http://opcfoundation.org/UA/RobotIntent/</ModelSourceGeneratorModelUri>
      <ModelSourceGeneratorPrefix>Opc.Ua.RobotIntent</ModelSourceGeneratorPrefix>
    </AdditionalFiles>
  </ItemGroup>
  <Import Project="..\..\tools\Opc.Ua.SourceGeneration\OPCFoundation.Opc.Ua.SourceGeneration.props" />
  <PropertyGroup>
    <ModelSourceGeneratorVersion>v105</ModelSourceGeneratorVersion>
    <ModelSourceGeneratorUseAllowSubtypes>true</ModelSourceGeneratorUseAllowSubtypes>
    <ModelSourceGeneratorOmitFluentApi>true</ModelSourceGeneratorOmitFluentApi>
  </PropertyGroup>
  <ItemGroup>
    <None Include="NugetREADME.md" Pack="true" PackagePath="\" />
  </ItemGroup>
</Project>
